public class Main {
   
   public static void main(String[] args) {
      Terrain ter;
      PhysicsEngine pe = new PhysicsEngine(ter);
      
      // read file and makes Terain object.
      // start the GUI
      
      // Reads the files of with moves
      
      for () {
         // A list of the x and y coordinates were the ball will "roll"
         ArrayList<double[]> motion = pe.getMotion(x, y, vx. vy);
         for (int i = 0; i < motion.size(); i++) {
            // GUI will plot all the locations which the ball visits
         }
      }
   }

   //ıt ıs main 
   
}